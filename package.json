{
    "private": true,
    "name": "crowdfunding-app",
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "lerna": "^5.1.2"
    },
    "scripts": {
        "start": "lerna run start",
        "test": "lerna run test",
        "new-version": "lerna version --conventional-commits --yes",
        "diff": "lerna diff"
    }

}
